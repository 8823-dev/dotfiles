#----------------------------------------
# 表示
#----------------------------------------
# カラースキーム
scheme set monokai

# フルパス ls
alias lp='ls -ld {$PWD}/{*,.*}'

#----------------------------------------
# 移動
#----------------------------------------
# Git リポジトリルートに移動
alias gr='cd $(git rev-parse --show-toplevel)'

